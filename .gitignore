node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.vercel
.env

# The bot is a standalone package: its lockfile is committed even though the
# root one is not, so `cd bot && npm install` is reproducible on the bot host.
!bot/package-lock.json

# Local Netlify folder
.netlify
.env*.local

# Credential / session artifacts - never commit
.env.vercel
.env*.local
.env.local
.env.production
.env.development
cookies.txt
fresh-cookies.txt
test-cookies.txt
vercel-env-example.txt
